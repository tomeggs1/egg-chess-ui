import ChessBoard from '../components/ChessBoard'

export default function PlayPage() {
  return (
    <section>
      <h1>Play</h1>
      <p>Board renders the starting position. Interactivity is not wired up yet.</p>
      <ChessBoard />
    </section>
  )
}
